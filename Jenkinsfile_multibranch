ipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls -l'
                sh 'hello world'
                sh 'ip -a'
                sh 'sudo apt-get update'
            }
        }
    }
}
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "building states"
                sh 'node -v'
                sh 'npm install'
            }
        }
    }
}